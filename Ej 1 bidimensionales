columnas=int(input("Diga el numero de columnas"))
filas=int(input("Diga el numero de filas"))
matriz=[[0 for _ in range(columnas)] for _ in range (filas)]
i=1
for fila in range (filas):
  for elemento in range(columnas):
    matriz[fila][elemento]=i
    i+=1
    print (matriz[fila][elemento])
